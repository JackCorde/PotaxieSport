<nav class="navbar navbar-expand px-4 py-3">
    <div class="navbar-collapse collapse d-flex justify-content-between align-items-center">

        <ul class="navbar-nav me-auto">
            <!-- Espacio para otros elementos a la izquierda -->
        </ul>

        <!-- Formulario de búsqueda (centrado) -->
        <form action="#" class="d-none d-sm-inline-block me-3">
            <!-- Contenido del formulario aquí -->
        </form>

        <!-- Sección derecha de la navbar -->
        <ul class="navbar-nav align-items-center">
            <li class="nav-item">
                <button type="button" class="toggle-sidebar-btn btn btn-secondary ms-3">
                    Catalogos
                </button>
            </li>
            <li class="nav-item">
                <button onclick="location.href='@Url.Action("Torneos", "Compartido")'" type="button" class="btn btn-secondary ms-3">
                    Torneos
                </button>
            </li>
            <li class="nav-item dropdown ms-3">
                <a href="#" data-bs-toggle="dropdown" class="nav-icon pe-md-0">
                    <i class="fa-duotone fa-solid fa-user fa-sm" style="--fa-primary-color: #fafcff; --fa-secondary-color: #d8dfe8;"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-end rounded">
                    <!-- Otros elementos del menú desplegable aquí -->
                    <a class="dropdown-item" href="/profile">Perfil</a>
                    <a class="dropdown-item" href="/settings">Configuraciones</a>
                    <div class="dropdown-divider"></div>
                    <form action="/logout" method="POST" class="px-3 py-1">
                        <button type="submit" class="btn btn-danger w-100" asp-action="CerrarSesion" asp-controller="Home">Cerrar Sesión</button>
                    </form>
                </div>
            </li>
        </ul>
    </div>
</nav>     